[project]
name = "my_crew_new"
version = "0.1.0"
description = "my_crew_new using crewAI"
authors = [{ name = "Your Name", email = "you@example.com" }]
requires-python = ">=3.10,<3.14"
dependencies = [
    "crewai[tools]==1.8.0"
]

[project.scripts]
my_crew_new = "my_crew_new.main:run"
run_crew = "my_crew_new.main:run"
train = "my_crew_new.main:train"
replay = "my_crew_new.main:replay"
test = "my_crew_new.main:test"
run_with_trigger = "my_crew_new.main:run_with_trigger"

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[tool.crewai]
type = "crew"
